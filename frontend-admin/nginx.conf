server {
  listen 80;
  server_name localhost;
  root /usr/share/nginx/html;
  index index.html;

  # 容器内以根路径提供静态文件，/admin 前缀由外层 nginx 剥离
  location / {
    try_files $uri $uri/ /index.html;
  }

  gzip on;
  gzip_types text/plain text/css application/json application/javascript text/xml;
}
